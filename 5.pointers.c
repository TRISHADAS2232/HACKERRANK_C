#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a;
    int b;
    scanf("%d", &a);
    scanf("%d", &b);
    printf("%d\n", a+b);
    printf("%d\n", abs(a-b));
    return 0;
}
